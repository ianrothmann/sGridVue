*
    box-sizing border-box
    margin 0
    padding 0

base-font-size = 16
gutter = 20px
columns = 12
cellClassName = 's-grid-cell'
gridClassName = 's-grid'

breakpoints = {
    xs: 0,
    sm: 640px,
    md: 1024px,
    lg: 1440px,
    xl: 1920px
}
